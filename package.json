{
  "name": "cloudinary-upload",
  "version": "1.0.0",
  "description": "upload image to cloudinary via API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "cantdocpp",
  "license": "MIT",
  "dependencies": {
    "cloudinary": "^1.17.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1"
  }
}
